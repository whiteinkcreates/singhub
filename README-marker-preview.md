# Marker Icon Refresh

This branch replaces the private room map marker with a more SingHUB-style room/door marker containing a microphone, and refreshes the live karaoke marker to use a matching inline SVG microphone instead of relying on emoji rendering.
